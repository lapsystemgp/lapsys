[
  {
    "kind": "title",
    "title": "The Backend — The Brain of TesTly",
    "subtitle": "The server that runs everything behind the scenes",
    "intro": "TesTly is a website and phone app where people in Egypt can find a medical lab, book a test, pay, and get their results. But the website and the app are just the screens you see. The backend is the brain that does the real work behind them.",
    "bullets": [
      "It listens to the web app and the phone app and answers their requests.",
      "It checks the rules (who is allowed to do what) and keeps data safe.",
      "It talks to the database, where all the information is stored.",
      "Same brain serves both the website and the phone app."
    ]
  },
  {
    "kind": "steps",
    "title": "What Does the Backend Actually Do?",
    "intro": "Think of it like a waiter between you and the kitchen.",
    "steps": [
      {
        "title": "1. It receives a request",
        "detail": "When you tap 'Book this test' on the app, a message is sent to the backend asking it to do something."
      },
      {
        "title": "2. It checks the rules",
        "detail": "Are you logged in? Are you allowed to do this? Is the lab real and open? It refuses anything that breaks the rules."
      },
      {
        "title": "3. It talks to the database",
        "detail": "The database is the big organised storage of all data (users, labs, bookings, results). The backend reads from it or saves new information into it."
      },
      {
        "title": "4. It sends back an answer",
        "detail": "It replies to the app with a clear result, like 'Booking confirmed' or 'That time is already taken'."
      }
    ]
  },
  {
    "kind": "diagram",
    "title": "Where the Backend Sits in the System",
    "image": "architecture",
    "imageCaption": "The web app and phone app both talk to one backend, which talks to the database and a few helper services.",
    "bullets": [
      "Two front doors (website + phone app), one shared brain.",
      "The backend is the only thing allowed to touch the database directly.",
      "Helper services (file storage, push notifications, AI) hang off the side."
    ]
  },
  {
    "kind": "cards",
    "title": "Tech Stack & Why We Chose It",
    "intro": "These are the main building blocks of the backend, and the reason for each.",
    "cards": [
      {
        "name": "NestJS",
        "tag": "Main framework",
        "what": "A toolkit for building the server in an organised, tidy way, splitting the code into neat sections called modules.",
        "why": "It keeps a big project from turning into spaghetti, with clear places to add login checks, rules, and features."
      },
      {
        "name": "TypeScript",
        "tag": "Programming language",
        "what": "JavaScript with type-checking, meaning it warns you when you use data the wrong way.",
        "why": "It catches many mistakes while we write code, before users ever see a bug."
      },
      {
        "name": "PostgreSQL (Neon)",
        "tag": "Database",
        "what": "The actual storage where all data lives. Neon is a cloud version that runs online without us managing a server.",
        "why": "It is reliable, well-tested, and great at keeping money and medical data correct and consistent."
      },
      {
        "name": "Prisma",
        "tag": "Database helper",
        "what": "A translator that lets our code talk to the database in plain, safe commands instead of raw database language.",
        "why": "It makes database code easier and safer to write, and blocks a whole class of hacking attacks."
      },
      {
        "name": "JWT + Passport",
        "tag": "Login / identity",
        "what": "JWT is a signed digital ticket that proves who you are. Passport is the helper that checks that ticket on every request.",
        "why": "It lets the server remember you are logged in without storing a session for every user, so it scales easily."
      },
      {
        "name": "bcrypt",
        "tag": "Password safety",
        "what": "A tool that scrambles passwords into unreadable text before saving them.",
        "why": "If the database were ever stolen, the real passwords still cannot be read."
      }
    ]
  },
  {
    "kind": "cards",
    "title": "Tech Stack & Why (continued)",
    "intro": "The helper services that handle files, alerts, documentation, and safety.",
    "cards": [
      {
        "name": "AWS S3",
        "tag": "File storage",
        "what": "An online storage locker for big files, used to keep the lab result PDF documents.",
        "why": "PDFs are too big to put in a normal database, and S3 stores them cheaply and safely at any scale."
      },
      {
        "name": "Firebase Cloud Messaging",
        "tag": "Push notifications",
        "what": "Google's service for sending pop-up alerts to phones, like 'Your result is ready'.",
        "why": "It is the standard, free, reliable way to reach both iPhone and Android phones."
      },
      {
        "name": "Swagger",
        "tag": "API documentation",
        "what": "Auto-generated, browsable documentation listing every command the backend understands.",
        "why": "It lets the team test and understand the backend without guessing, saving lots of time."
      },
      {
        "name": "class-validator",
        "tag": "Input checking",
        "what": "A tool that checks incoming data is the right shape (a real email, a price that is a number, etc.).",
        "why": "It stops bad or broken data at the front door before it can cause errors deeper in the system."
      }
    ]
  },
  {
    "kind": "diagram",
    "title": "Inside the Backend: The Modules",
    "image": "component-backend",
    "imageCaption": "The backend is split into clear sections, each handling one job.",
    "bullets": [
      "Auth handles login and who-you-are.",
      "Bookings handles ordering and paying for tests.",
      "Results handles the result PDFs and the health-trend data.",
      "Each section is small and focused, so it is easy to understand and fix."
    ]
  },
  {
    "kind": "steps",
    "title": "How Login & User Roles Work",
    "intro": "How the backend knows who you are and what you are allowed to do.",
    "imageCaption": "Login gives you two tickets, and guards check them on every request.",
    "steps": [
      {
        "title": "1. You log in",
        "detail": "You send your email and password. The backend uses bcrypt to check the password matches the scrambled one it stored."
      },
      {
        "title": "2. You get two tickets",
        "detail": "An access token (a short-lived ticket valid for 8 hours that proves who you are) and a refresh token (a long-lived ticket used to get a new access token later)."
      },
      {
        "title": "3. Every request shows the ticket",
        "detail": "The website carries the ticket in a secure cookie; the phone app carries it in the request header. One backend understands both."
      },
      {
        "title": "4. Guards check your role",
        "detail": "There are three roles: Patient, Lab Staff, and Admin. 'Role guards' make sure a patient can't open lab-only pages, and a not-yet-approved lab can't sell tests."
      }
    ]
  },
  {
    "kind": "steps",
    "title": "How Booking + Payment Works",
    "intro": "What happens from picking a time slot to paying for the test.",
    "image": "seq-booking",
    "imageCaption": "The patient books a slot, the backend locks it in safely, then payment and confirmation follow.",
    "steps": [
      {
        "title": "1. See free time slots",
        "detail": "The app asks the backend which appointment times are still open at the chosen lab, and shows them to the patient."
      },
      {
        "title": "2. Create the booking",
        "detail": "The backend checks the rules (the lab is open, the test exists, the slot is free) and works out the price, including any home-visit fee."
      },
      {
        "title": "3. Lock the slot safely",
        "detail": "It saves the booking in one safe all-or-nothing step so the same time slot can never be grabbed by two people at once."
      },
      {
        "title": "4. Pay, then confirm",
        "detail": "The patient pays online or with cash. The lab then confirms the booking, and the patient gets a notification. An online booking can only be confirmed after it is paid."
      }
    ]
  },
  {
    "kind": "steps",
    "title": "How Lab Results Get Delivered",
    "intro": "How a test result travels from the lab to the patient, safely.",
    "image": "seq-result",
    "imageCaption": "The lab uploads the PDF and the numbers, then the patient is notified and can download it.",
    "steps": [
      {
        "title": "1. Lab uploads the official PDF",
        "detail": "The backend double-checks the file really is a PDF (not just named like one) and keeps it under 10 MB, then stores it in S3."
      },
      {
        "title": "2. Lab adds the numbers",
        "detail": "The lab also enters the actual readings (like glucose level). These get cleaned up into a standard form for health trends."
      },
      {
        "title": "3. Mark as delivered",
        "detail": "The lab marks the result delivered. The booking is now complete and the patient gets a 'Result Ready' notification."
      },
      {
        "title": "4. Patient downloads it",
        "detail": "Only the right patient, lab, or admin can open the file. The backend checks this every time and never shares the raw storage link."
      }
    ]
  },
  {
    "kind": "diagram",
    "title": "How the Data Is Organised",
    "image": "erd",
    "imageCaption": "Every user, lab, booking, and result is a table, linked together. A Booking sits at the center.",
    "bullets": [
      "A booking connects a patient, a lab, and a test together.",
      "Each booking can have a result file and structured readings.",
      "Storing data in linked tables keeps it tidy and prevents contradictions."
    ]
  },
  {
    "kind": "algorithm",
    "title": "Clever Bit 1: Comparing Results Across Different Labs",
    "intro": "Different labs write the same test in different names and different units. So a patient who switches labs can't easily see if their health is improving or getting worse. We fix that by converting everything to one common form.",
    "steps": [
      {
        "title": "Match the name",
        "detail": "'Fasting glucose', 'FBS', and 'Fasting Blood Sugar' all mean the same thing. The backend uses a dictionary to recognise them as one marker."
      },
      {
        "title": "Convert the unit",
        "detail": "One lab reports glucose as 5.4 mmol/L, another as 97 mg/dL. The backend converts both to the same unit using fixed medical formulas (for glucose, multiply by 18)."
      },
      {
        "title": "Flag anything unsure",
        "detail": "If a unit isn't recognised, it is marked 'not comparable' instead of guessing, so a wrong number never goes on the patient's health chart."
      }
    ],
    "why": "It lets a patient see one honest health trend over time, even when their tests came from several different labs."
  },
  {
    "kind": "algorithm",
    "title": "Clever Bit 2: Two People Can't Grab the Same Slot",
    "intro": "Imagine two patients tap 'Book' on the exact same 10:00 AM slot at the exact same moment. Without protection, both could be booked and the lab is double-booked. We make that impossible.",
    "steps": [
      {
        "title": "A database rule: one slot, one booking",
        "detail": "The database is told that each time slot can be linked to only one active booking. It will physically refuse a second one."
      },
      {
        "title": "All-or-nothing save (a transaction)",
        "detail": "The booking is saved as a single bundled step. If anything goes wrong, the whole thing is undone, never half-saved."
      },
      {
        "title": "Loser gets a friendly message",
        "detail": "Whoever's request arrives second is cleanly rejected with 'That slot is already booked', instead of a crash or a double-booking."
      }
    ],
    "why": "It guarantees the schedule is always correct, even when many people book at the very same second."
  },
  {
    "kind": "algorithm",
    "title": "Clever Bit 3: Staying Logged In, Safely",
    "intro": "We want users to stay logged in for a long time without re-typing their password constantly. But a long-lived login ticket is dangerous if stolen. The trick is to rotate the ticket.",
    "steps": [
      {
        "title": "Short ticket + long ticket",
        "detail": "The access ticket expires fast (8 hours). The refresh ticket lasts longer (30 days) and is used quietly to get a fresh access ticket."
      },
      {
        "title": "Rotate on every use",
        "detail": "Each time the refresh ticket is used, the old one is thrown away and a brand-new one is issued. The old one stops working immediately."
      },
      {
        "title": "Stored scrambled",
        "detail": "Only a scrambled fingerprint of the refresh ticket is saved, never the real one, so a stolen database is useless to an attacker."
      }
    ],
    "why": "If a refresh ticket is ever stolen, it works at most once before it is rotated out, sharply limiting the damage."
  },
  {
    "kind": "codesnip",
    "title": "Code: How Distance Is Calculated",
    "intro": "Every lab has a latitude and longitude stored in the database. The backend calculates the straight-line distance to the user using the haversine formula — a standard formula that works on a curved Earth, not a flat map.",
    "file": "apps/backend/src/public/public-utils.ts",
    "url": "https://github.com/lapsystemgp/lapsys/blob/main/apps/backend/src/public/public-utils.ts",
    "code": "export function haversineKm(\n  lat1: number, lng1: number,\n  lat2: number, lng2: number\n): number {\n  const R = 6371;  // Earth radius in km\n  const toRad = (deg: number) => (deg * Math.PI) / 180;\n\n  const dLat = toRad(lat2 - lat1);\n  const dLng = toRad(lng2 - lng1);\n\n  const a =\n    Math.sin(dLat / 2) ** 2 +\n    Math.cos(toRad(lat1)) *\n    Math.cos(toRad(lat2)) *\n    Math.sin(dLng / 2) ** 2;\n\n  const km = R * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));\n  return Math.round(km * 10) / 10;  // round to 1 decimal\n}",
    "note": "**R = 6371** is the mean radius of the Earth in kilometres. The formula converts the lat/lng difference into radians, computes an intermediate value `a` (a squared half-chord length on the sphere), then uses `atan2` to convert it to an arc length. The result is multiplied by R to get kilometres. The same formula runs identically in TypeScript (frontend) and Dart (mobile app) — so every client shows the same distance for the same lab."
  },
  {
    "kind": "codesnip",
    "title": "Code: How Fuzzy Search Works",
    "intro": "When a user types 'HBA1' instead of 'HbA1c', or 'glucos' instead of 'glucose', the search still finds the right test. It uses two techniques: Levenshtein edit distance for typos, and PostgreSQL trigrams for partial matches.",
    "file": "apps/backend/src/common/utils/search.utils.ts",
    "url": "https://github.com/lapsystemgp/lapsys/blob/main/apps/backend/src/common/utils/search.utils.ts",
    "code": "// Levenshtein edit distance — counts minimum character changes\n// between two strings (insert, delete, or substitute one char).\nfunction editDistance(a: string, b: string): number {\n  const m = a.length, n = b.length;\n  const prev = Array.from({ length: n + 1 }, (_, j) => j);\n  const curr = new Array<number>(n + 1);\n  for (let i = 1; i <= m; i++) {\n    curr[0] = i;\n    for (let j = 1; j <= n; j++) {\n      curr[j] = a[i-1] === b[j-1]\n        ? prev[j-1]\n        : 1 + Math.min(prev[j], curr[j-1], prev[j-1]);\n    }\n    prev.splice(0, n+1, ...curr);\n  }\n  return prev[n];\n}\n\n// Returns true if token fuzzy-matches anywhere in text.\n// Allowed edits: 1 for tokens >=4 chars, 2 for >=6 chars.\nexport function fuzzyContains(text: string, token: string): boolean {\n  const t = token.toLowerCase(), s = text.toLowerCase();\n  if (s.includes(t)) return true;  // exact substring wins\n  const maxDist = t.length >= 6 ? 2 : t.length >= 4 ? 1 : 0;\n  if (maxDist === 0) return false;\n  return s.split(/\\s+/).some(w => editDistance(t, w) <= maxDist);\n}",
    "note": "**Edit distance = 1** means one character changed: 'glucos' → 'glucose' (insert 'e'). **Edit distance = 2** handles bigger typos: 'haemogloin' → 'haemoglobin'. Short tokens (under 4 chars) require an exact match to avoid false positives. The database also uses **pg_trgm** (`word_similarity`) in parallel — it splits words into 3-character chunks and scores overlap. Both techniques run together so the search catches both typos and partial words."
  },
  {
    "kind": "codelinks",
    "title": "Explore the Backend Code",
    "intro": "All backend source code is on GitHub. Click any card to open that file or folder directly.",
    "repo": "https://github.com/lapsystemgp/lapsys",
    "links": [
      {
        "path": "apps/backend/src/",
        "label": "Backend source root",
        "desc": "All NestJS modules: auth, bookings, labs, results, chat, public, admin, FAQ",
        "url": "https://github.com/lapsystemgp/lapsys/tree/main/apps/backend/src"
      },
      {
        "path": "apps/backend/src/chat/chat.service.ts",
        "label": "AI Assistant service",
        "desc": "Gemini function-calling loop, SSE streaming, tool runners (find_labs, search_tests)",
        "url": "https://github.com/lapsystemgp/lapsys/blob/main/apps/backend/src/chat/chat.service.ts"
      },
      {
        "path": "apps/backend/src/common/utils/search.utils.ts",
        "label": "Smart search engine",
        "desc": "Fuzzy search, Levenshtein edit distance, pg_trgm relevance ranking, synonym expansion",
        "url": "https://github.com/lapsystemgp/lapsys/blob/main/apps/backend/src/common/utils/search.utils.ts"
      },
      {
        "path": "apps/backend/src/common/utils/synonyms.ts",
        "label": "Medical synonym map",
        "desc": "Maps 'CBC' → 'complete blood count', 'HbA1c' → 'السكر التراكمي', and 40+ medical term groups",
        "url": "https://github.com/lapsystemgp/lapsys/blob/main/apps/backend/src/common/utils/synonyms.ts"
      },
      {
        "path": "apps/backend/src/public/public-utils.ts",
        "label": "Haversine distance",
        "desc": "Great-circle distance formula that calculates km between user and each lab",
        "url": "https://github.com/lapsystemgp/lapsys/blob/main/apps/backend/src/public/public-utils.ts"
      },
      {
        "path": "apps/backend/src/auth/auth.service.ts",
        "label": "Auth & JWT tokens",
        "desc": "bcrypt password check, access/refresh token issuance, token rotation, role guards",
        "url": "https://github.com/lapsystemgp/lapsys/blob/main/apps/backend/src/auth/auth.service.ts"
      },
      {
        "path": "apps/backend/src/bookings/bookings.service.ts",
        "label": "Booking & payment logic",
        "desc": "Slot availability, race-condition prevention, status machine (Pending→Confirmed→Completed)",
        "url": "https://github.com/lapsystemgp/lapsys/blob/main/apps/backend/src/bookings/bookings.service.ts"
      },
      {
        "path": "apps/backend/prisma/schema.prisma",
        "label": "Database schema",
        "desc": "All tables: User, LabProfile, LabTest, Booking, BookingResult, Conversation, ChatMessage…",
        "url": "https://github.com/lapsystemgp/lapsys/blob/main/apps/backend/prisma/schema.prisma"
      }
    ]
  },
  {
    "kind": "qa",
    "title": "Q&A: Backend Deep Questions",
    "intro": "Common questions from developers reading the code for the first time.",
    "pairs": [
      {
        "q": "How does the search avoid SQL injection?",
        "a": "The search uses Prisma's `$queryRaw` with **tagged template literals**. Each `${value}` in the template becomes a parameterized placeholder — the database driver escapes it automatically. No string is ever concatenated into the SQL text, so there is nothing for an attacker to inject."
      },
      {
        "q": "What is `word_similarity` and why not just LIKE?",
        "a": "**word_similarity** is a PostgreSQL function from the `pg_trgm` extension. It splits words into overlapping 3-character chunks (trigrams) and scores how many chunks two strings share. `LIKE '%cbc%'` only finds exact substrings. `word_similarity` also catches 'CBC' vs 'C.B.C', partial words, and minor variations. We run both in parallel: LIKE catches exact hits, pg_trgm scores fuzzy relevance."
      },
      {
        "q": "Why is the similarity threshold 0.3?",
        "a": "0.3 is a balance tested against real medical terms. Lower (e.g. 0.1) returns too many irrelevant results. Higher (e.g. 0.5) misses legitimate short abbreviations. 'CBC' vs 'complete blood count' scores ~0.2 similarity (the words are totally different), so the synonym map expands the query first — **then** trigrams catch typos within each synonym."
      },
      {
        "q": "How does booking prevent two people grabbing the same slot at the same moment?",
        "a": "The database has a **unique constraint** on (lab_id, appointment_time, status='Active'). Both users send their request at the same time. The backend wraps the insert in a Prisma transaction. One insert succeeds; the second violates the unique constraint and gets a database error. The backend catches that error and returns a clean '409 Slot already taken' response — no double-booking, no crash."
      },
      {
        "q": "Why store only a hash of the refresh token?",
        "a": "If the database were ever breached, an attacker with plain refresh tokens could impersonate any user indefinitely. By storing only a **bcrypt/SHA-256 hash**, the attacker gets hashes they cannot reverse into real tokens. The real token lives only on the user's device and in the HTTP response — it never sits in the database in readable form."
      },
      {
        "q": "How are uploaded PDFs validated?",
        "a": "The backend checks two things: (1) the file's **MIME type** must be `application/pdf` (not just the filename extension, which anyone can fake), and (2) the file size must be under 10 MB. The first few bytes of every PDF file start with `%PDF-` — the backend reads the buffer header to confirm this before accepting the upload."
      }
    ]
  },
  {
    "kind": "title",
    "title": "Putting It All Together",
    "subtitle": "One brain, many safe jobs",
    "intro": "The backend is the trusted middle layer that makes TesTly work. Everything a patient or lab does passes through it, gets checked, and is stored correctly.",
    "bullets": [
      "One backend serves both the website and the phone app.",
      "Layered safety: login check, role check, lab-approval check, and ownership check.",
      "Smart algorithms make results comparable, bookings race-proof, and logins secure.",
      "Built to handle real money and real medical data carefully."
    ]
  }
]
